<%# form_for(@new_post) do |f| %>
<h3>Reply to thread</h3>
<%= form_for(@new_post, :html => { :multipart => true }) do |f| %>
  <% if @new_post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@new_post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% @new_post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <%= f.hidden_field :topic_id, value:@new_post.topic_id.to_s %>
  <div class="form-group row">
    <%= f.label :content, "content", class:"col-sm-2 form-control-label"%>
    <div class="col-sm-10">
    <%= f.text_area :content,class:"form-control" %>
    </div>
  </div>
  <!--div class="form-group row">
    <label for="inputEmail3" class="col-sm-2 form-control-label">Email</label>
    <div class="col-sm-10">
      <input type="email" class="form-control" id="inputEmail3" placeholder="Email">
    </div>
  </div -->
  <div class="form-group row">
    <%= f.label :content, "content", class:"col-sm-2 form-control-label"%>
    <div class="col-sm-10">
    <div style="width: 30%">
    <%= f.select :show_name, Post.show_names.keys,class:"form-control" %>
    </div><div style="width: 69%;">
    <%= f.text_field :author,class:"form-control" %>
    </div>
    </div>
  </div>
  <div class="field">
    <%= f.label :show_name %><br>
    <%= f.select :show_name, Post.show_names.keys %>  </div>
  <div class="field">
    <%= f.label :author %><br>
    <%= f.text_field :author %>
  </div>
  <div class="field">
    <%= f.label :file %><br>
    <%# f.file_field :file, :multiple => true, name: "post_attachments[file][]" %>
    <%= f.file_field :attachment %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
